import { Flex, Select } from "@chakra-ui/react";

const LeadsList = () => {
  return (
    <Flex>
      <Select placeholder="Select a list" maxW={"500px"} borderRadius="md" />
    </Flex>
  );
};

export default LeadsList;
