import { Card, CardHeader, CardTitle } from "@/components/ui/card";

const XPaymentCard = () => {
  return (
    <Card>
      <CardHeader className="text-center">
        <CardTitle>X Payments</CardTitle>
      </CardHeader>
    </Card>
  );
};

export default XPaymentCard;
